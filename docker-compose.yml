version: "3.3"
services:
  mqtt: 
    image: eclipse-mosquitto
    container_name: mqtt
    ports:
      - "1883:1883"
  # rabbitmq:
  # kafka:
  # grpc:
  nats:
    image: nats
    container_name: nats
    ports:
      - "4222:4222"
  # redis: