{
  "name": "@ogma/common",
  "description": "A package for the @ogma packages to have centralized typings.",
  "files": [
    "lib"
  ],
  "scripts": {
    "prebuild": "rimraf lib",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.2.3"
  }
}
